// Todo Delete this after building out layouts directory
const Body = (initialVnode) => {


    return {
        view: (vNode) => {
            const {attrs, children} = vNode;


            return children
        }
    }
}

export default Body;