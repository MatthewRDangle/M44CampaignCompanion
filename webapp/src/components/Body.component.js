import m from 'mithril';


const Body = (initialVnode) => {


    return {
        view: (vNode) => {
            const {attrs, children} = vNode;


            return children
        }
    }
}

export default Body;