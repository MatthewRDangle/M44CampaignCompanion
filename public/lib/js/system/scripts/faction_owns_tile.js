"use strict"; // Execute JavaScript file in strict mode.

var faction_owns_tile = undefined;
(function() {

    /**
     * @function
     * @description Checks if a faction is the owner of a tile.
     */
    faction_owns_tile = function(scene, emitter, scenario) {

    }
})();